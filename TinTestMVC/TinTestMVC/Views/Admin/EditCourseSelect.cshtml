@model IEnumerable<Application.DTO.CourseDetailsDto>

@{
    ViewData["Title"] = "Wybierz kurs do edycji";
}

<div class="form-container">
    <h2 class="form-title">Wybierz kurs do edycji</h2>

    <form asp-action="EditCourse" method="get" class="admin-form">
        <div class="form-group">
            <label for="id" class="form-label">Kurs</label>
            <select name="id" class="form-input" required>
                <option value="">-- Wybierz kurs --</option>
                @foreach (var course in Model)
                {
                    <option value="@course.Id">@course.Title</option>
                }
            </select>
        </div>

        <button type="submit" class="form-button">Edytuj</button>
    </form>
</div>
